---
title: TileData
description: "Tile data stored per cell per layer. Not an ECS component -- tiles are pure data managed by TerrainData, not entities."
---

`Omnith.Sim`

Tile data stored per cell per layer. Not an ECS component -- tiles are pure data managed by TerrainData, not entities.

