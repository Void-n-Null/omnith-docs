---
title: PathfindingSystem
description: "Computes A* paths when an entity's PathTarget changes. Threaded: paths are computed in parallel, results applied sequentially."
---

`Omnith.Ecs.Systems`

Computes A* paths when an entity's PathTarget changes. Threaded: paths are computed in parallel, results applied sequentially.

