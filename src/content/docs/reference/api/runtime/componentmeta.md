---
title: ComponentMeta
description: "Assigns a unique integer ID to each component type at runtime. The static generic pattern gives O(1) lookup with zero dictionary overhead."
sidebar:
  badge:
    text: Runtim
---

`Omnith.Ecs`

Assigns a unique integer ID to each component type at runtime. The static generic pattern gives O(1) lookup with zero dictionary overhead.

## Properties

| Name | Description |
|---|---|
| `Count` | Total number of registered component types. |

