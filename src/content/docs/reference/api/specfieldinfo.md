---
title: SpecFieldInfo
description: "Cached reflection info for a spec type. Built once per type, reused for every instance. Separates spec-reference fields (any field whose type inherits from Spec"
---

`Omnith.Data`

Cached reflection info for a spec type. Built once per type, reused for every instance. Separates spec-reference fields (any field whose type inherits from Spec) from primitive fields.

